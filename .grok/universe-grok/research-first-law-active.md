# Universe Grok - Permanent Mandatory Research-First Law Activation Marker

**Status**: ACTIVE

This file was created by the SessionStart hook.

## Critical Instruction to the Agent

The existence of this marker + the truth-layer file
`.universe-grok/truth/permanent-mandatory-research-first-law-for-recommendation-queries.md`
means the following law is in full force for this entire session:

**The FORCED CURRENT TRUTH LAYER INJECTION printed by this same hook at session start contains the full permanent law file (plus CURRENT-TRUTH and NAMING).** The bootstrap sentence ("Bootstrap complete...") must have been written in your first reasoning block before you even evaluate whether any query triggers this law. All law-triggered research (web_search + x_search) and answers must still be derived exclusively from the Current Truth layer's context + the fresh external results. The law file in `.universe-grok/truth/` remains the sole authority for its triggers. Read the injected version first.

This law triggers on **two** classes of queries (semantic intent controls):

1. Any user question asking for the recommended, proper, best, current, or "right" way to do something (tooling, hooks, configuration, architecture, process, etc.).
2. Any query that names or inquires about the current state / detailed mechanics / implementation of a specific technical system or component whose precise current details are not guaranteed to be in the agent's reliable knowledge (classic example: "grok 의 Hooks 시스템이 어떻게 됨", "how does the Hooks system currently work", etc.). The mere presence of such a named uncertain subsystem forces the mandatory sentence + fresh `web_search` + `x_search` before any local reasoning or answer.

Skipping this is a first-order Truth-Seeking betrayal of the project.
No exceptions. No "I already knew". No "this is just an explanation request". No rationalization.

The law file in `.universe-grok/truth/` is the sole authority. Read the injected copy from the hook output.
