{
  "description": "UserPromptSubmit hook that forces fresh web_search + x_search for every user prompt via artifact + additionalContext injection. This is the fundamental mechanism for per-turn external research (replaces previous static law approach).",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash hooks/user-prompt-submit.sh",
            "timeout": 15
          }
        ]
      }
    ]
  }
}